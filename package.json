{
  "name": "excalidraw-selfhosted",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm -w backend run dev & npm -w frontend run dev",
    "build": "npm -w backend run build && npm -w frontend run build",
    "lint": "npm -w backend run lint && npm -w frontend run lint"
  }
}

